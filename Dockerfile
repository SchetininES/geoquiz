# Используем легковесный Nginx
FROM nginx:alpine

# Копируем все файлы проекта в папку, из которой Nginx раздает статику
# Файлы из .dockerignore сюда НЕ попадут!
COPY . /usr/share/nginx/html

# Порт 80 открыт по умолчанию в образе nginx, так что просто запускаем
